import SwiftUI

extension Color {
    /// Primary brand color
    static let appPrimary = Color(red: 0.2, green: 0.4, blue: 0.8)
    
    /// Background color - white in light mode, dark gray in dark mode
    static let appBackground = Color(red: 1.0, green: 1.0, blue: 1.0)
}

